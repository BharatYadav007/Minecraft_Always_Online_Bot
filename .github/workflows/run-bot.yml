name: Run Minecraft Bot

on:
  workflow_dispatch:
  #schedule:
    #- cron: "0 */4 * * *"

concurrency:
  group: minecraft-bot
  cancel-in-progress: false

jobs:
  run-bot:
    runs-on: ubuntu-latest
    environment: online-bot
    timeout-minutes: 245
    env:
      MINECRAFT_SERVER_IP: ${{ vars.MINECRAFT_SERVER_IP }}
      MINECRAFT_SERVER_PORT: ${{ vars.MINECRAFT_SERVER_PORT }}
      MINECRAFT_BOT_NAME: ${{ vars.MINECRAFT_BOT_NAME }}
      MINECRAFT_PASSWORD: ${{ secrets.MINECRAFT_PASSWORD }}
      MINECRAFT_AUTH: ${{ vars.MINECRAFT_AUTH }}
      MINECRAFT_AUTO_NIGHT_SKIP: ${{ vars.MINECRAFT_AUTO_NIGHT_SKIP }}
      MINECRAFT_LOGIN_MESSAGE: ${{ vars.MINECRAFT_LOGIN_MESSAGE }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Validate startup script
        run: npm run check

      - name: Run bot
        run: npm start
